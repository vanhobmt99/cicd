version: 0.2
 
phases:
  install:
    runtime-versions:
  nodejs:
    version: latest
    commands:
        - echo Installing dependencies...
        - npm install
 
  build:
    commands:
        - echo Build started on `date`
        - echo Compiling the Node.js code...
        - node app.js
 
artifacts:
  files:
    - '**/*'
  discard-paths: yes